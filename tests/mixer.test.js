const assert=require('node:assert/strict');const fs=require('node:fs');const vm=require('node:vm');const source=fs.readFileSync(require('node:path').join(__dirname,'..','mixer.js'),'utf8');const context={module:{exports:{}},document:{getElementById:()=>null,querySelectorAll:()=>[]},window:{},console};vm.runInNewContext(source,context);const {mixPaintHex,normaliseWeights}=context.module.exports;
assert.equal(normaliseWeights([50,50,0]).join(','),'50,50,0');assert.equal(normaliseWeights([25,25,50]).join(','),'25,25,50');assert.equal(mixPaintHex([{hex:'#FF0000'},{hex:'#0000FF'}],[50,50]),'#BC00BC');assert.equal(mixPaintHex([{hex:'#FFFFFF'},{hex:'#000000'}],[50,50]),'#BCBCBC');console.log('Spectrum Paint mixer tests passed');
